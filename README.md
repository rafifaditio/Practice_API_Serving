"# Practice_API_Serving"

Notes docker command #Windows

docker-compose -f composeFile.yml build

docker-compose -f composeFile.yml down

docker-compose -f composeFile.yml up -d

docker-compose -f composeFile.yml logs

docker ps

docker images
